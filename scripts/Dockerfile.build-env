FROM ubuntu:24.04

# Build helper image for sandbox disk generation.
# apt verifies package signatures using Ubuntu's trusted keyring.
RUN apt-get update && \
    apt-get install -y --no-install-recommends ca-certificates curl qemu-utils && \
    rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
